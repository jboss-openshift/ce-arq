{
  "kind": "RoleBinding",
  "apiVersion": "v1",
  "metadata": {
    "name": "${ROLE_REF_NAME}",
    "creationTimestamp": null
  },
  "userNames": [
    "${USER_NAME}"
  ],
  "groupNames": [],
  "subjects": [
    {
      "kind": "ServiceAccount",
      "namespace": "${NAMESPACE}",
      "name": "${SUBJECT_NAME}"
    }
  ],
  "roleRef": {
    "name": "${ROLE_REF_NAME}"
  }
}
